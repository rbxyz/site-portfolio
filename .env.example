DATABASE_URL="banco"
AUTH_SECRET="your-secret-key-here"

# Separate database for the /blog module (reused from the /dev/blog project).
# The portfolio only READS from it — never run migrations against this DB.
BLOG_DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/blogdb?sslmode=require"